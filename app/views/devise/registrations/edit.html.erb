<% provide(:title, 'プロフィールを編集') %>

<div class="user_edit">
  <div class="edit_container">
    <%= render "devise/registrations/edit_form" %>
  </div>
</div>

<div class="user_cancel">
  <h4>アカウントを削除する</h4>
  <%= button_to "削除", registration_path(resource_name) , :class => "btn btn-primary", data: { confirm: "Are you sure?" }, method: :delete %>
</div>

<%= link_to :back, class: 'back'  do %>
 　<i class="fas fa-arrow-circle-left"></i>戻る
<% end %>